services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: saveslate
      POSTGRES_PASSWORD: saveslate
      POSTGRES_DB: saveslate
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U saveslate"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://saveslate:saveslate@db:5432/saveslate
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    volumes:
      - ./backend:/app
    restart: unless-stopped

volumes:
  pgdata:
